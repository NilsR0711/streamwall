// @fontsource/* packages are imported only for their CSS/font side effects and
// ship no type declarations. Declare them as side-effect-only modules so
// TypeScript's stricter module checks (vite-react preset) don't flag them.
declare module '@fontsource/noto-sans'
declare module '@fontsource/*'
